<h3> Here are All of our students</h3>

<ul>
<% @students.each do |s| %>
    <li><%= link_to s.to_s, student_path(s.id) %></li>
    <% end %>
</ul>


<div><%= link_to "Create a New Student", new_student_path %></div>